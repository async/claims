{
  "$schema": "../../../schema/claims.schema.json",
  "$comment": "Each anchor must appear verbatim in README.md. Test mappings live in claims.coverage.json.",
  "claims": [
    {
      "id": "readme.basic-example",
      "source": "README.md",
      "anchor": "The coverage registry maps the README promise to the `PROMISE: ` test title in `tests/basic.test.js`."
    }
  ]
}
