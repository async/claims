# Basic Node Test Example

This example uses `@async/claims` without any agent loop.

```sh
pnpm install
pnpm claims:check
```

The registry maps the README promise to the `PROMISE: ` test title in `tests/basic.test.js`.
