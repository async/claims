{
  "name": "async-claims-basic-node-test",
  "private": true,
  "type": "module",
  "scripts": {
    "claims:check": "pnpm run pipeline:claims",
    "pipeline:claims": "async-claims check",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@async/claims": "file:../.."
  },
  "engines": {
    "node": ">=24"
  }
}
