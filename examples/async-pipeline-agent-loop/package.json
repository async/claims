{
  "name": "async-claims-pipeline-agent-loop",
  "private": true,
  "type": "module",
  "scripts": {
    "claims": "async-pipeline run-task claims",
    "claims-report": "async-pipeline run-task claims-report",
    "claims-repair": "ASYNC_AGENT=mock async-pipeline run-task claims-repair",
    "patch:check": "git apply --check claims.patch"
  },
  "devDependencies": {
    "@async/claims": "file:../..",
    "@async/pipeline": "^0.2.3"
  },
  "engines": {
    "node": ">=24"
  }
}
